<%= form_for(@scheduler) do |f| %>
  <% if @scheduler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scheduler.errors.count, "error") %> prohibited this scheduler from being saved:</h2>

      <ul>
      <% @scheduler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color_id %><br>
    <%= f.collection_select(:color_id, Color.order(:name), :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>